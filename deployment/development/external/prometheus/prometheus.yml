---
global:
  scrape_interval: 5s #1m
  evaluation_interval: 5s #1m

alerting:

rule_files:

scrape_configs:
  - job_name: backend-service-template
    static_configs:
      - targets: ["app:8080"] # metric port